resource_types:
  - name: slack-notification
    type: docker-image
    source:
      repository: cfcommunity/slack-notification-resource
resources:
  - name: slack-alert
    type: slack-notification
    source:
      url: {{slack-url}}
  - name: travis-resource-git
    type: git
    source:
      uri: https://github.com/Orange-OpenSource/travis-resource.git
      branch: master
  - name: travis-resource-image
    type: docker-image
    source:
      email: {{docker-hub-email}}
      username: {{docker-hub-username}}
      password: {{docker-hub-password}}
      repository: {{docker-hub-image}}
jobs:
  - name: publish-travis-resource-image
    public: true
    serial: true
    plan:
    - get: travis-resource-git
      trigger: true
    - task: build-resource
      file: travis-resource-git/build.yml
      on_failure:
        put: slack-alert
        params:
          text_file: notif-resource/slack-notification.txt
          text: "$TEXT_FILE_CONTENT"
          channel: {{slack-channel}}
          username: {{slack-username}}
    - put: travis-resource-image
      params:
        build: built-resource/
    - put: slack-alert
      params:
        text_file: notif-resource/slack-notification.txt
        text: "$TEXT_FILE_CONTENT"
        channel: {{slack-channel}}
        username: {{slack-username}}